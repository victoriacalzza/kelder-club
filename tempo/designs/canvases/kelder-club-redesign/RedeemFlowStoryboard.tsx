import { RedeemFlow } from "@/components/modals/RedeemFlow";

export default function RedeemFlowStoryboard() {
  return (
    <div className="relative h-full w-full bg-ink-50">
      <RedeemFlow onClose={() => {}} />
    </div>
  );
}
