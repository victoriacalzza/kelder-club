import { QRModal } from "@/components/modals/QRModal";

export default function QRModalStoryboard() {
  return (
    <div className="relative h-full w-full bg-ink-50">
      <QRModal onClose={() => {}} />
    </div>
  );
}
